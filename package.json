{
    "name": "basel-analyzer",
    "version": "1.0.0",
    "description": "basel framework vscode extension",
    "main": "./out/extension.js",
    "scripts": {
        "test": "jest",
        "vscode:prepublish": "npm run compile",
        "compile": "tsc -p ./",
        "lint": "eslint \"src/**/*.ts\"",
        "watch": "tsc -watch -p ./"
    },
    "repository": {
        "type": "git",
        "url": "y"
    },
    "author": "Josh",
    "license": "ISC",
    "devDependencies": {
        "@types/jest": "^26.0.23",
        "@types/node": "^16.0.0",
        "@types/vscode": "^1.73.0",
        "jest": "^27.0.6",
        "ts-jest": "^27.0.3",
        "ts-node": "^10.0.0",
        "typescript": "^4.3.5"
    },
    "engines": {
        "vscode": "^1.73.0"
    },
    "categories": [
        "Other"
    ],
    "activationEvents": [
        "*"
    ],
    "jest": {
        "preset": "ts-jest",
        "testEnvironment": "node"
    }
}
